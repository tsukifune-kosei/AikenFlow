# SimpleVault Aiken Contracts

Generated by AikenFlow.

This output is intentionally readable. Review the generated constraints before using the validators in production.

The contracts are generated from AikenFlow IR. Repository outlines and agent-context files are not used as compiler semantics.

## Layout

- `aiken.toml` contains Aiken package metadata and dependencies.
- `validators/` contains application validators.

## Validators

- `Locked` handles transitions: Withdraw, EmergencyCancel

## Build

```bash
aiken check
aiken build
```
